# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
jinja2==3.1.5
aiofiles==24.1.0
websockets==14.2

# Task queue
celery[redis]==5.4.0
redis==5.2.1

# Database
sqlalchemy==2.0.36
alembic==1.14.1

# Pydantic / Config
pydantic==2.10.4
pydantic-settings==2.7.1

# Scene detection
scenedetect[opencv]==0.6.5
opencv-python-headless==4.10.0.84

# Export
lxml==5.3.0

# Utilities
python-dotenv==1.0.1
httpx==0.28.1
Pillow==11.1.0

# AI/ML — GPU packages (installed with CUDA support)
# These are installed separately in the Dockerfile for proper CUDA linking:
# torch, torchvision, open-clip-torch, ultralytics, openai-whisper
